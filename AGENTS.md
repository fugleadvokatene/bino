- Only edit source files. Do not modify generated/compiled artifacts that are checked in - these include `*_templ.go` files (generated from `.templ` sources), `models.go` and `db.go` at the repo root (generated by sqlc), and any file with a `// Code generated` header.
- You may run tests (`go test ./...`) and rebuild as needed. Use `just <recipe>` for builds and codegen - e.g. `just templ` to regenerate templ files, `just gen` for all codegen. Do not call tool CLIs directly (e.g. `templ generate`) as they may be a different version than what go.mod pins.
- When creating pull requests, do not put a comment in the pull request.